enable: true

listen:
  host: "0.0.0.0"
  port: 3002
  path: "/"

upstream:
  url: "ws://127.0.0.1:2536/OneBotv11/ws"

# SnowLuma → ws-Adapter 与 ws-Adapter → TRSS 共用此 Token。
accessToken: ""
connectTimeout: 5000
debug: false
